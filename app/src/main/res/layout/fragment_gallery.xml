<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:ignore="MissingClass">

    <!-- ① 雨夜背景层（最底层） -->
    <com.xi.app.ui.view.RainyGlassView
        android:id="@+id/rainyBg"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- ② 全局朦胧遮罩：让整体有隔着玻璃看的雾感 -->
    <View
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@drawable/bg_frost_overlay" />

    <!-- ③ 内容容器（毛玻璃卡片 + Header + RecyclerView） -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <!-- Header 区域：毛玻璃卡片 -->
        <androidx.cardview.widget.CardView
            android:id="@+id/headerCard"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="40dp"
            android:layout_marginEnd="16dp"
            app:cardBackgroundColor="@color/xi_frost_card_bg"
            app:cardCornerRadius="20dp"
            app:cardElevation="0dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent">

            <!-- 毛玻璃内部渐变叠加 -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:background="@drawable/bg_glass_card"
                android:orientation="vertical"
                android:paddingStart="20dp"
                android:paddingTop="18dp"
                android:paddingEnd="20dp"
                android:paddingBottom="18dp">

                <!-- 装饰线 -->
                <View
                    android:layout_width="36dp"
                    android:layout_height="3dp"
                    android:layout_marginBottom="12dp"
                    android:background="@color/xi_amber"
                    android:backgroundTint="@color/xi_amber" />

                <TextView
                    android:id="@+id/galleryHeader"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:fontFamily="serif"
                    android:text="画廊回顾"
                    android:textColor="@color/xi_amber"
                    android:textSize="26sp"
                    android:textStyle="bold"
                    android:letterSpacing="0.08" />

                <TextView
                    android:id="@+id/gallerySubtitle"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="6dp"
                    android:text="岁月留下的文字，皆是光的裂缝"
                    android:textColor="@color/xi_text_secondary"
                    android:textSize="12sp"
                    android:letterSpacing="0.04" />

            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <!-- RecyclerView 区域 -->
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/galleryRecycler"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="16dp"
            android:clipToPadding="false"
            android:paddingStart="50dp"
            android:paddingEnd="50dp"
            android:paddingBottom="80dp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/headerCard" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.constraintlayout.widget.ConstraintLayout>